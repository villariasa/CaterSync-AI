# Admin Screens

Screens
- Admin Dashboard (KPI cards, recent activity)
- Booking List (table with filters)
- Booking Detail (timeline + actions)
- Inventory List and Item Detail

Components
- DataTable with pagination and CSV export
- Calendar view for bookings
- Filters panel with date range, status, tenant selector

API Needs
- `GET /api/bookings`
- `GET /api/inventory`
- `GET /api/analytics/kpis`
