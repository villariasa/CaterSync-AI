# API Documentation

API documentation will live here as endpoints are implemented. The initial backend exposes health checks and JWT token endpoints:

- `GET /api/health/`
- `POST /api/auth/token/`
- `POST /api/auth/token/refresh/`
- `POST /api/auth/token/verify/`
